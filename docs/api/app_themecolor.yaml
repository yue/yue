name: App::ThemeColor
description: Represent possible theme colors.

lang_detail:
  cpp: |
    This type is an `enum class` with following values:
      * `App::ThemeColor::Text`

  lua: |
    This type is an `string` with following possible values:
    * `"text"`

  js: |
    This type is an `String` with following possible values:
    * `"text"`
