loadable_module("node_yue") {
  output_name = "yue"
  output_extension = "node"
  output_prefix_override = true  # do not add "lib" prefix

  sources = [
    "init.cc",
    "lifetime.cc",
    "lifetime.h",
  ]

  deps = [
    "//nativeui",
    "//v8binding",
  ]
}
